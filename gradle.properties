org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# App versioning (shared between phone and watch modules)
appVersionName=1.9.5
appVersionCodePhone=109
appVersionCodeWatch=108

# Release notes for Google Play (max 500 chars, written during version bump)
releaseNotes=Fix stale version and config sync on phone companion. \
Improve tile responsiveness: eliminate double state refresh, parallel server race, cached theme. \
Debug log now captured reliably across app lifecycle. \
Fix transparent background on tile action confirmation dialog.
