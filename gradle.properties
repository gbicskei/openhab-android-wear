org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# App versioning (shared between phone and watch modules)
appVersionName=1.9.6
appVersionCodePhone=115
appVersionCodeWatch=114

# Release notes for Google Play (max 500 chars, written during version bump)
releaseNotes=Fix watch version and status fields being overwritten by phone sync race. \
Watch now re-asserts its authoritative status after every settings sync and on startup. \
Preserve appVersionCode during phone settings write. \
Upload script improvements.
