org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# App versioning (shared between phone and watch modules)
appVersionName=1.9.7
appVersionCodePhone=117
appVersionCodeWatch=116

# Release notes for Google Play (max 500 chars, written during version bump)
releaseNotes=Server selector now logs probe response times and whether the local preference window was used, making connection diagnostics clearer in the debug log.
