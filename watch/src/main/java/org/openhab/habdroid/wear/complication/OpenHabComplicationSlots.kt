package org.openhab.habdroid.wear.complication

import dagger.hilt.android.AndroidEntryPoint

/** Complication slot 1. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot1Service : OpenHabSlotComplicationService() {
    override val slotNumber = 1
}

/** Complication slot 2. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot2Service : OpenHabSlotComplicationService() {
    override val slotNumber = 2
}

/** Complication slot 3. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot3Service : OpenHabSlotComplicationService() {
    override val slotNumber = 3
}

/** Complication slot 4. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot4Service : OpenHabSlotComplicationService() {
    override val slotNumber = 4
}

/** Complication slot 5. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot5Service : OpenHabSlotComplicationService() {
    override val slotNumber = 5
}

/** Complication slot 6. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot6Service : OpenHabSlotComplicationService() {
    override val slotNumber = 6
}

/** Complication slot 7. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot7Service : OpenHabSlotComplicationService() {
    override val slotNumber = 7
}

/** Complication slot 8. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot8Service : OpenHabSlotComplicationService() {
    override val slotNumber = 8
}

/** Complication slot 9. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot9Service : OpenHabSlotComplicationService() {
    override val slotNumber = 9
}

/** Complication slot 10. Configured via phone app. */
@AndroidEntryPoint
class OpenHabComplicationSlot10Service : OpenHabSlotComplicationService() {
    override val slotNumber = 10
}
